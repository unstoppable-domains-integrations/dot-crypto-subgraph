{
  "name": "subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ unstoppabledomains/subgraph",
    "create-local": "graph create --node http://localhost:8020/ unstoppabledomains/subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ unstoppabledomains/subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 unstoppabledomains/subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.26",
    "typescript": "^4.1.5"
  }
}
